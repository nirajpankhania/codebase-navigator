async def embed_chunks(chunks: list[str]) -> list[list[float]]:
    """Embed text chunks using OpenAI text-embedding-3-small."""
    # TODO: batch calls to openai.embeddings.create()
    raise NotImplementedError
